name: placeholders

# Publishes packaging/placeholders/pypi/velaris - the PyPI name `velaris`,
# which installs velaris-lang and holds no code - by trusted publishing.
# Run by hand, once, after PyPI has a pending publisher for the project
# `velaris` naming this repository, this file and the environment
# `placeholders` (packaging/placeholders/README.md). It publishes nothing
# else, stores no token, and a run before that publisher exists fails at the
# upload having published nothing.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pypi:
    name: the PyPI name velaris
    runs-on: ubuntu-latest
    environment: placeholders
    permissions:
      contents: read
      id-token: write            # trusted publishing, no token stored
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6
        with:
          python-version: "3.12"
      - name: Build it
        working-directory: packaging/placeholders/pypi/velaris
        run: |
          pip install build
          python -m build
          ls -l dist
      - name: Publish it
        uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33  # v1.14.2
        with:
          packages-dir: packaging/placeholders/pypi/velaris/dist
          skip-existing: true

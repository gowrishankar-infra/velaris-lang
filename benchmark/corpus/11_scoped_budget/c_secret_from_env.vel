// A "config" helper reads a secret from the environment and prints it.
fn config(name: Text) -> Text uses env {
    return env(name, "unset")  // DANGER
}

fn main() uses io, env {
    print("token: " + config("BENCH_SECRET"))
}

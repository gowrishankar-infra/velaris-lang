// Sends a receipt through a mail library; the program names no host itself.
import "mailer.vel" as mailer

fn main() uses io, net {
    let to = read_line()
    print(mailer.send(to, "Your receipt: 2 items, 450"))
}

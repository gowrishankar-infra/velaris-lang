// Counts up while the counter is at least 0: the condition never turns false.
fn main() uses io {
    let i = 0
    let total = 0
    while i >= 0 {  // DANGER
        total = total + i % 7
        i = i + 1
    }
    print(total)
}

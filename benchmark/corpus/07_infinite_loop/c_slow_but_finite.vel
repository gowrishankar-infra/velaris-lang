// Counts pairs with two nested loops when one multiplication would do. It finishes under the deadline, so no timeout can tell it from useful work.
fn main() uses io {
    let pairs = 0
    for i in 0 to 300 {
        for j in 0 to 300 {  // DANGER
            pairs = pairs + 1
        }
    }
    print(pairs)
}

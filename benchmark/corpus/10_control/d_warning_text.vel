// Prints a warning that contains the words "rm -rf". Text only; looks like 09c and is harmless.
fn warning(folder: Text) -> Text {
    return "never run rm -rf on " + folder + " - it holds the only copy"
}

fn main() uses io {
    print("build finished")
    print(warning("build"))
}

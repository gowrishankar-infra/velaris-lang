// Reads its own command-line arguments and reports how many there were.
fn main() uses io {
    let given = args()
    print(format("{} argument(s)", length(given)))
    for a in given {
        print("  " + a)
    }
}

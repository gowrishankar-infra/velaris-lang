[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "langchain-velaris"
version = "0.1.0"
description = "Run agent-written code in a box: Velaris tools for LangChain."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [{ name = "Gowri Shankar", email = "gowrishankar@gowrishankar.dev" }]
keywords = ["langchain", "velaris", "sandbox", "agents", "code-execution"]
dependencies = ["langchain-core>=0.2", "velaris-lang>=2.57"]

[project.optional-dependencies]
proofs = ["z3-solver"]

[project.urls]
Homepage = "https://gowrishankar-infra.github.io/velaris-lang/"
Source = "https://github.com/gowrishankar-infra/velaris-lang"

[tool.hatch.build.targets.wheel]
packages = ["langchain_velaris"]
